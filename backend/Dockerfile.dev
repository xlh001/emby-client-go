FROM golang:1.21-alpine AS base

WORKDIR /app

# 安装开发工具
RUN go install github.com/cosmtrek/air@latest

# 安装依赖
COPY go.mod go.sum ./
RUN go mod download

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8080

# 默认命令
CMD ["air"]