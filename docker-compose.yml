version: '3.8'

services:
  # Emby管理平台
  emby-client-go:
    image: xlh001/emby-client-go:latest
    container_name: emby-client-go
    ports:
      - "8080:8080"
    volumes:
      # 持久化数据
      - ./data:/root/data
      # 配置文件挂载
      - ./configs:/root/configs
    environment:
      # 服务器配置
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - SERVER_MODE=release
      # 数据库配置
      - DATABASE_TYPE=sqlite
      - DATABASE_DATABASE=./data/emby_manager.db
      # JWT配置
      - JWT_SECRET=请修改为安全的密钥
      - JWT_EXPIRE_TIME=86400
      - JWT_ISSUER=emby-manager
      # 日志配置
      - LOG_LEVEL=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - emby-client-go-network

  # PostgreSQL数据库（可选）
  postgres:
    image: postgres:16-alpine
    container_name: emby-client-go-postgres
    environment:
      POSTGRES_DB: emby_manager
      POSTGRES_USER: emby
      POSTGRES_PASSWORD: 请设置安全的密码
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U emby"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - emby-client-go-network
    profiles:
      - postgres

  # Redis缓存（可选）
  redis:
    image: redis:7-alpine
    container_name: emby-client-go-redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - emby-client-go-network
    profiles:
      - redis

  # Nginx反向代理（生产环境推荐）
  nginx:
    image: nginx:alpine
    container_name: emby-client-go-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./logs/nginx:/var/log/nginx
    depends_on:
      - emby-client-go
    restart: unless-stopped
    networks:
      - emby-client-go-network
    profiles:
      - nginx

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  emby-client-go-network:
    driver: bridge