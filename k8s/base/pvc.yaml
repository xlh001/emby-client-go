apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emby-manager-data
  namespace: emby-manager
  labels:
    app: emby-manager
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: fast-ssd  # 根据集群配置调整